# Reference Files

Saved copies of templates and UI designs for easy review.

| File | Description |
|------|-------------|
| `templates/login.html` | LifeVault login page (purple gradient, card layout) |

These files are not used by Django at runtime. The live templates live in `accounts/templates/`.
